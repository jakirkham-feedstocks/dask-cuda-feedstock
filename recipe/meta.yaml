{% set name = "dask-cuda" %}
{% set version = "22.12.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/dask-cuda-{{ version }}.tar.gz
  sha256: 9f6cab004c77feb460842d5174f9a1496931f6211d8d5e7469a1dc2614a7d76b

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - dask-cuda-worker = dask_cuda.cli.dask_cuda_worker:go

requirements:
  host:
    - python >=3.8
    - pip
    - versioneer
  run:
    - python >=3.8
    - dask ==2022.11.1
    - distributed ==2022.11.1
    - pynvml >=11.0.0
    - numpy >=1.18.0
    - numba >=0.54
    - pandas >=1.0
    - zict >=0.1.3
    - __linux

test:
  imports:
    - dask_cuda
    - dask_cuda.benchmarks
  commands:
    - pip check
    - dask-cuda-worker --help
  requires:
    - pip

about:
  home: https://github.com/rapidsai/dask-cuda
  summary: Utilities for Dask and CUDA interactions
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ocefpaf
    - jakirkham
